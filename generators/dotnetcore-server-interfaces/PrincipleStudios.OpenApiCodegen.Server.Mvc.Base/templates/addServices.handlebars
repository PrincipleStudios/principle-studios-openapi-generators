{{>partial_header header}}
{{#with this as |root|}}
namespace Microsoft.Extensions.DependencyInjection
{
    public static partial class OpenApiControllerRegistrationExtensions
    {
        public static IMvcBuilder AddOpenApi{{methodName}}<{{#each controllers}}T{{className}}Controller{{#unless @last}}, {{/unless}}{{/each}}>(this IServiceCollection services){{#each controllers}}
            where T{{className}}Controller : global::{{root.packageName}}.{{className}}ControllerBase{{/each}}
        {
            return services.AddControllers().AddNewtonsoftJson();
        }
    }
}
{{/with}}