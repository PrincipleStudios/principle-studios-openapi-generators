{{>partial_header header}}
#pragma warning disable
#nullable enable
#nullable disable warnings

namespace {{this.packageName}}
{
    {{#with model}}
    {{#if description}}/// <summary>
    /// {{linewrap description "
    /// "}}
    /// </summary>
    {{/if}}public partial record {{className}}({{#each vars}}{{! TODO: support parent properties}}
        [property: global::System.Text.Json.Serialization.JsonPropertyName("{{baseName}}"){{#if required}}, global::System.ComponentModel.DataAnnotations.Required{{/if}}]{{!
    data type: }} {{{dataType}}}{{!
    name: }} {{name}}{{#unless @last}}, {{/unless}}
    {{/each}}){{#if parent}} : {{{parent}}}(){{/if}};
    {{/with}}
}
