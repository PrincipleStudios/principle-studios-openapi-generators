{{>partial_header header}}

{{#with model}}
    /**{{#if Description}}
        * {{{Description}}}
    {{/if}}
    * @export
    * @interface {{ClassName}}
    */
    export const {{ConstName}} = [
    {{#each EnumVars}}
        {{{value}}},
    {{/each}}
    ] as const;
    export type {{ClassName}} = typeof {{ConstName}}[number];
    
{{/with}}
