/* eslint-disable */
{{>partial_header header}}
{{#if imports}}
import {
{{#each imports}}
{{{.}}},
{{/each}}
} from './';

{{/if}}
/**{{#if description}}
 * {{{description}}}{{/if}}
 * @export
 * @interface {{classname}}
 */
export interface {{classname}} {{#if parent}}extends {{{parent}}} {{/if}}{
{{#if additionalPropertiesType}}
    [key: string]: {{{additionalPropertiesType}}}{{#if}} | any{{/if}}; {{! TODO - we really don't want `| any` here... see if TypeScript has an update for us. }}
{{/if}}
{{#each vars}}
    /**{{#if description}}
     * {{{description}}}{{/if}}
     * @type {{{datatype}}}
     * @memberof {{classname}}
     */
{{#if isReadOnly}}readonly {{/if}}{{name}}{{#unless required}}?{{/unless}}: {{{datatype}}}{{#if isNullable}} | null{{/if}};
{{/each}}
}
